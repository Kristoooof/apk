[package]
name = "ep2pc-proto"
version = "0.1.0"
edition = "2021"
description = "EP2PC wire types: Envelope + payloads + group control (EP2PC-005, EP2PC-006)"

[dependencies]
prost = "0.12"
bytes = "1"

[dev-dependencies]
rand = "0.8"

